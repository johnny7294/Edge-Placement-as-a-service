The script will run the awk to match a particular pattern in the lines and prints 
the count of the occurences in the file.

This script will be used by the tenant to search for movie names and count.
If the tenant wants the detailed info about the requests, then use grep command on 
the log file. ---path /var/log/nginx/access.log